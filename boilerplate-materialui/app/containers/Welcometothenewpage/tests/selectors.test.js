// import { fromJS } from 'immutable';
// import { makeSelectWelcometothenewpageDomain } from '../selectors';

// const selector = makeSelectWelcometothenewpageDomain();

describe('makeSelectWelcometothenewpageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
