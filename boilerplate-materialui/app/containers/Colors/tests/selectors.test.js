// import { fromJS } from 'immutable';
// import { makeSelectColorsDomain } from '../selectors';

// const selector = makeSelectColorsDomain();

describe('makeSelectColorsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
